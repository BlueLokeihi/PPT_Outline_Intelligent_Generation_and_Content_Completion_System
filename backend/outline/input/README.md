# 输入文本文件夹

- 把“主题提示词/资料文本”放到这个目录下，例如：`my_topic.txt`。
- 建议使用 UTF-8 编码。
- 运行时通过 CLI 指定：

```powershell
python backend/run_outline.py --input backend/inputs/my_topic.txt --config backend/config/models.json
```
